apiVersion: v1
kind: Service
metadata:
  name: {{ include "camunda-client-app.fullname" . }}
  labels:
    {{- include "camunda-client-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "camunda-client-app.selectorLabels" . | nindent 4 }}
